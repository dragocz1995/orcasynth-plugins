{
  "name": "web",
  "version": "0.1.0",
  "apiVersion": "1",
  "description": "Web research: web_search (Tavily) and web_fetch (fetch a page as readable text). Modeled on the Hermes web toolset.",
  "entry": "index.mjs",
  "provides": { "tools": ["web_search", "web_fetch"] },
  "configSchema": [
    { "key": "tavilyApiKey", "label": "Tavily API key", "type": "secret", "hint": "app.tavily.com — powers web_search. Without it only web_fetch works." },
    { "key": "maxResults", "label": "Search results", "type": "number", "hint": "How many results web_search returns (default 5)." }
  ]
}
